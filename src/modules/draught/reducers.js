/*import * as actionTypes from './actionTypes'
import { UPDATE_SELECTED_DRAUGHT } from '../board'
// LOOKS LIKE DRAUGHT DONT EVEN NEED A REDUCER SINCE IT ALWAYS UPDATE THE TILE THAT CONTAINS THE DRAUGHT
const initialState = {
	draught: undefined
}

const reducer = (state = initialState, payLoad) => {
	switch (payLoad.type) {
		case actionTypes.SELECT_DRAUGHT: {
			return {
				...state,
				draught: payLoad.draught
			}
		}
		default:
			return state
	}
}

export default reducer*/
